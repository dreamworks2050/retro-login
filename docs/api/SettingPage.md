# SettingPage

## Namespace

`Retrologin`

## Methods

| Method | Description |
|--------|-------------|
| `init`  | The `SettingPage` class. Serves as an example on how to manage the plugin setting. It shows how to add a submenu on WordPress admin, enqueue the scripts and styles, and rendering the setting page. Feel free to remove this or modify it to suit your needs. |
| `registerSettings`  | The `SettingPage` class. Serves as an example on how to manage the plugin setting. It shows how to add a submenu on WordPress admin, enqueue the scripts and styles, and rendering the setting page. Feel free to remove this or modify it to suit your needs. |
| `addMenu`  | Add the settings menu on WordPress admin. |
| `enqueueAdminScripts($$adminPage)`  |  |
| `getInlineScript`  | Load the inital data to show on the admin page. |

