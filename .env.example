# =============================================================================
# RetroLogin WordPress Plugin - Environment Variables Template
# =============================================================================
# Copy this file to .env and update with your local values
# =============================================================================

# WordPress Development Settings
# -----------------------------------------------------------------------------
WP_DEBUG=true
WP_DEBUG_LOG=true
WP_DEBUG_DISPLAY=false
SCRIPT_DEBUG=true

# WordPress Database (LocalWP default values)
# -----------------------------------------------------------------------------
# Note: LocalWP manages these automatically. Only needed for custom setups.
# DB_NAME=local
# DB_USER=root
# DB_PASSWORD=root
# DB_HOST=localhost
# DB_CHARSET=utf8mb4
# DB_COLLATE=

# WordPress URLs
# -----------------------------------------------------------------------------
# WP_HOME=http://retrologin.local
# WP_SITEURL=http://retrologin.local

# Plugin Development
# -----------------------------------------------------------------------------
# Set to true to enable verbose error logging for plugin
RETROLOGIN_DEBUG=false

# Build & Asset Settings
# -----------------------------------------------------------------------------
# NODE_ENV is set automatically by build scripts (production/development)
# NODE_ENV=development

# Testing (if PHPUnit is configured)
# -----------------------------------------------------------------------------
# WP_TESTS_DIR=/path/to/wordpress-tests-lib
# WP_CORE_DIR=/path/to/wordpress

# =============================================================================
# Security Note: Never commit actual .env file to version control
# The .gitignore already excludes .env files
# =============================================================================
